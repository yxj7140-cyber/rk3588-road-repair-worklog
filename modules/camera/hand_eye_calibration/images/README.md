# Calibration Images

This folder is a placeholder for small selected examples only.

Raw calibration image sets can become large. Keep the full capture set local and record the path in the result JSON instead of committing all images.
