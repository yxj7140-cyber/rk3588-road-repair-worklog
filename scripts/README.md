# Scripts

Scripts are separated by purpose:

- `test/`: experimental or repeatable tests.
- `recovery/`: restore, reflashing, image repair, and board recovery.
- `host-windows/`: Windows-side helpers.
- `vm-linux/`: VM-side Linux helpers.

Do not put one-off board-resident scripts here unless they are also saved with a date, purpose, and result.
